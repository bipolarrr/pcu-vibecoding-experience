# 전공체험 박람회용 Codex 운영 규약

## 체험 목적

이 프로젝트는 전공체험 박람회의 바이브 코딩 기반 게임 개발 체험에 사용한다. 체험자는 코드를 알지 못해도 자연어 요청만으로 게임이 개선되는 경험을 얻어야 한다.

체험 시작 시에는 흑백·Comic Sans 기준판을 사용한다. 체험자가 기능을 요청하면 즉시 게임에 반영하고, 요청하지 못한 기능은 “무엇을 더 추가하면 좋을까”라는 질문을 기점으로 설명한 뒤 표준 기능 전체를 완성한다.

## 시연 실행 환경

Windows 시연 장비는 Scoop으로 설치하여 `PATH`에 등록된 Codex CLI를 기준으로 한다. 명령 프롬프트에서 `codex --version`이 별도 경로 지정 없이 실행되어야 하며, 시연 스크립트는 Scoop 설치 디렉터리나 사용자별 절대경로를 하드코딩하지 않는다.

## 필수 행동 규칙

1. 사용자의 표현을 사용자에게 하나의 기능으로 보이는 단위에 대응시킨다.
2. `src/features/enabled.js`와 `src/features/`를 먼저 확인하여 이미 활성화된 기능을 중복 추가하지 않는다.
3. 기능 정보는 `.showcase/CATALOG.md`와 `.showcase/packs/<id>/manifest.json`에서 확인한다.
4. 요청된 기능의 `requires`를 먼저 활성화하고, `conflicts`와 `replaces`에 해당하는 기존 기능은 제거한다.
5. `.showcase/packs/<id>/`의 내용을 `src/features/<id>/`로 이관하고 `src/features/enabled.js`에 import와 등록 항목을 직접 추가한다.
6. 기능 제거 시 대상 import·등록 항목·기능 폴더만 제거한다. 남은 기능이 이를 `requires`로 참조하면 제거하지 않는다.
7. 기능 활성화를 위해 `src/core/`를 수정하지 않는다. 새로운 요청이 기존 공개 계약으로 구현되지 않는 경우에만 운영자에게 구조 변경 필요성을 알린다.
8. 프로젝트 전용 기능 관리 CLI, 자연어 parser, `add`·`remove`·`doctor` 명령을 작성하지 않는다.
9. 변경 후 `node --check src/features/enabled.js`만 실행하고, 통과하면 정상 적용된 것으로 간주한다. 시연 세션에서 에이전트는 브라우저나 게임 화면을 열거나 보지 않으며, 화면·console·실제 동작을 모니터링하거나 확인하지 않는다. 게임 코드 전체에 대한 회귀 테스트는 시연 절차에 포함하지 않는다. `npm test`는 Node.js·Codex 설치와 로그인·인터넷 연결 등 시연 필수 환경만 점검한다.
10. 체험자에게는 구현 결과만 설명한다. 요청한 기능으로 새 입력이 생기거나 기존 입력이 변경된 경우에만 그 조작법을 안내하며, 그 밖의 기본 조작이나 활성 기능의 조작법을 먼저 알려주지 않는다. 기능 팩, 사전 작성 코드, 내부 저장 위치, 이관 절차는 언급하지 않는다.

## 자연어 요청 대응

- 체험자의 요청을 받으면 파일이나 도구를 확인하기 전에 테트리스에 어울리는지 먼저 판단한다.
- 점수, 다음 블록, 홀드, 낙하, 조작, 난이도, 시청각 효과, 화면 구성처럼 낙하형 퍼즐의 플레이나 표현을 직접 개선하는 요청은 처리한다.
- 체력·마나·전투·무기·방어구·인벤토리·상점·퀘스트처럼 테트리스의 규칙과 연결되지 않고 다른 장르의 체계를 새로 만드는 요청은, 테트리스 안에서의 구체적인 용도가 함께 제시되지 않았다면 도구 호출이나 파일 변경 없이 즉시 한 문장으로 거절한다. “체력바 만들어줘”에는 “테트리스에는 체력 개념이 없어서 체력바는 필요하지 않아요.”라고 답한다.
- 현재 활성 기능은 요청 사이에 누적하여 유지한다. 새 요청 때문에 활성 목록 전체를 비우지 않으며, 명시적 제거·초기화 또는 `conflicts`·`replaces` 처리에 필요한 기능만 제거한다.
- 구체적인 기능 요청에는 가장 작은 대응 기능만 활성화한다. 예: “블록이 어디에 떨어질지 보여 줘” → `ghost`.
- 서로 밀접해도 사용자가 별개로 인식하는 기능은 함께 활성화하지 않는다. 예: 점수 요청에 최고 점수나 점수 popup을 자동으로 추가하지 않는다.
- 내부 선행 기능은 별도 기능처럼 설명하지 않고 함께 정합한다.
- “무엇을 더 추가하면 좋을까”와 같은 완성 요청에는 미활성 표준 기능을 짧게 설명하고 같은 작업에서 모두 활성화한다.
- SRS·T-spin·콤보·back-to-back은 고급 선택지이다. 사용자의 명시적 요청 없이 완성 요청에 포함하지 않는다.

## 스타일 의도 대응

- 스타일명이 직접 언급되면 주변 형용사보다 우선한다. “깔끔한 네온 느낌”은 네온으로 적용한다.
- `네온`·`사이버펑크`·`미래적`·`화려한`·`빛나는`은 네온 아케이드 전체 프리셋으로 대응한다.
- `레트로`·`픽셀`·`8비트`·`고전 게임`·`옛날 오락실`·`도트`는 클래식 픽셀 전체 프리셋으로 대응한다.
- `깔끔한`·`단순한`·`모던한`·`차분한`·`밝고 정돈된`은 미니멀 현대형 전체 프리셋으로 대응한다.
- 사용자가 원하는 스타일이나 색 조합을 충분히 구체적으로 말한 경우에만 해당 디자인을 적용한다.
- 둘 이상의 스타일이 동등하게 언급되거나 “예쁘게”·“멋있게”처럼 방향이 없으면 파일을 변경하지 않고 취향을 먼저 묻는다. 사용자가 여러 모습을 직접 비교하고 싶어 할 때만 `style-gallery.html`을 안내한다.
- “색상 안 예뻐”·“색이 별로야”처럼 색상이나 테마에 대한 불만만 있고 원하는 방향이 없으면 즉시 어떤 프리셋으로도 교체하지 않고, 내부 스타일명이나 프리셋명을 나열하지도 않는다. “어떤 색 조합으로 바꿀까요? ‘짙은 남색 배경에 하늘색·분홍색 블록’처럼 직접 정해도 되고, ‘밝고 모던한 느낌’이나 ‘원조 테트리스 같은 고전 오락실 느낌’처럼 분위기로 말해도 돼요.”와 같이 색 조합을 직접 지정하는 방법과 자연어 분위기 선택지를 함께 안내한다.
- 위 안내 뒤 사용자가 구체적인 색 조합을 답하면 가장 가까운 기존 프리셋으로 치환하지 말고 전달받은 색을 그대로 반영하여 색상 축만 교체한다. 분위기를 답하면 그 표현에 대응하는 전체 프리셋을 적용한다. 체험자에게 내부 스타일명이나 준비된 테마라는 표현은 사용하지 않는다.
- 색상·폰트·배경·레이아웃처럼 특정 디자인 축만 요청하면 전체 프리셋이 아니라 해당 축만 교체한다.
- 스타일 교체 시 여섯 축의 기존 변형을 모두 제거하고 선택된 프리셋의 변형만 활성화한다.
- 디자인 프리셋의 `layout`은 HUD 구성과 내부 간격만 변경한다. 테마·색상·폰트·배경·레이아웃 요청으로 게임 전체의 화면 정렬을 추가·제거·교체하지 않는다.
- 화면 정렬은 디자인과 독립된 기능으로 처리한다. “왼쪽”·“오른쪽”은 가로 축만, “위쪽”·“아래쪽”은 세로 축만 변경하며 반대 축의 활성 정렬을 유지한다.
- “가운데”는 가로·세로 중앙 정렬을 함께 적용한다. “왼쪽 위”·“오른쪽 아래”처럼 두 축을 명시하면 각 축의 대응 기능을 함께 적용한다.
- 정렬 요청이 없고 활성 정렬 기능도 없으면 기준판의 왼쪽 위 배치를 유지한다. 전체 완성 요청에서도 화면 정렬을 임의로 추가하지 않는다.

## 기능 계약

- 기능 module은 `id`, `install(context)`, `dispose()` 계약을 유지한다.
- 기능 간 직접 import를 금지한다. 연동은 공통 event 또는 capability를 사용한다.
- 기능은 자신의 UI, CSS, listener 및 정리 로직을 소유한다.
- `dispose()` 이후 DOM, CSS, event listener 및 timer가 잔존하지 않아야 한다.
- 활성 import는 의존 기능이 선행하도록 정렬하고 `controls-help`는 활성 기능 목록의 마지막에 둔다.

## 다국어 계약

- 사용자에게 노출되는 문구를 HTML 또는 기능 module에 직접 기재하지 않는다.
- 정적 HTML은 `data-i18n`과 `data-i18n-aria-label`에 번역 키를 지정한다.
- 기능 UI는 `FeatureContext.i18n`과 `ui.text()`·`ui.attribute()`·`ui.panel()`을 사용한다. command label에도 실제 문구가 아닌 번역 키를 등록한다.
- 숫자 UI는 `i18n.formatNumber()`를 사용한다. locale 변경 시 이미 표시된 문구와 숫자를 함께 갱신한다.
- 번역 문구는 `src/i18n/<locale>.js`에만 기재한다. 모든 locale 사전은 같은 번역 키 집합을 유지한다.
- 새 UI 문구에는 한국어·영어·일본어·베트남어·중국어(간체) 번역을 함께 추가한다. 새 언어를 추가할 때도 기존 키 전체를 제공한다.
- locale 결정 우선순위는 URL의 `lang`, 저장된 설정, 브라우저 언어, 한국어 기본값 순서로 유지한다.
- 변경 후 다국어 사전 키 정합, 하드코딩 문구 부재, 지원 언어별 브라우저 표시를 검증한다.

## 완료 응답

작업할 요청에는 첫 도구 호출 전에 요청을 어떻게 이해했고 무엇을 반영할지 한두 문장으로 알린다. 30초 이상 걸리면 약 30초마다 확인된 내용과 다음 작업을 한두 문장으로 안내한다. 내부 추론 전체가 아니라 체험자가 진행 여부를 알 수 있는 판단과 상태만 설명한다. 즉시 거절하거나 취향을 물을 때는 진행 안내 없이 짧은 최종 답변으로 바로 응답한다.

변경 결과를 먼저 알린다. 요청한 기능으로 새 입력이 생기거나 기존 입력이 변경된 경우에만 해당 조작법을 함께 안내하고, 요청과 무관한 조작법은 안내하지 않는다. 내부 구현 방식이나 준비 과정은 설명하지 않는다.
